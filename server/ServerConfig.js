// Stałe serwera

const SERVER_DELAY = 500 // Opóźnienie tego co jest na planszy gracza względem serwera 
const POLLING_INTERVAL = 200 // Czas co jaki wysyłane są dane na socket
module.exports = { SERVER_DELAY, POLLING_INTERVAL }
